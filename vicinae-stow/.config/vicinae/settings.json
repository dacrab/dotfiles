// Merged with vicinae's default config; values here take precedence.
{
   "$schema": "https://vicinae.com/schemas/config.json",
   "theme": {
      "dark": {
         "name": "nord",
         "icon_theme": "Papirus-Dark"
      }
   },
   "favorites": [
      "applications:com.spotify.Client",
      "applications:helium"
   ]
}